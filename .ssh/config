# SSH config

# Explicitly disable agent forwarding
ForwardAgent no

# Include local-machine-specific options
# This file should not be versioned as it may contain sensitive information
Include ~/.ssh/config.d/localhost
